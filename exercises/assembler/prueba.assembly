`LDS, `R0, 8'hf8;
`LDI, `R1, 8'b00000000;
`ADDI, `R0, 8'h00;

;Si NF es 0, rotamos sin mas. Si NF es 1, le añadimos el 1
//Comentario
`BRPL,       8'h05;


;Caso en el que el sign bit era 1
`LDI, `R1, 8'b10000000;

;Rotacion
`CLC, 8'h00;
`ROR,  `R0, 8'h00;
`ADD,  `R0, 5'd0, `R1;

`STS, `R0, 8'hf1;
`STOP, 11'd0;